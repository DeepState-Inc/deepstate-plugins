{
  "name": "deepstate-plugins",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "registry": "node scripts/build-registry.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
